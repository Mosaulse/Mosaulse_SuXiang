---
alwaysApply: true
scene: git_message
---

# 素缃 · AI 提交信息生成规则

> 素缃一卷，墨香千年。
> AI 提交，意境悠然。

每条 commit message 遵循以下格式：
> 类型 简短的古典化描述 （可选）详细说明，延续文白相间的风格

**类型标签：**
| 标签 | 含义 |
|------|------|
| 墨 | 代码逻辑、配置结构变更 |
| 色 | 配色调整、色值修改 |
| 笺 | 文档、README、注释更新 |
| 印 | 版本发布、标签 |
| 裁 | 依赖管理、构建工具 |
| 校 | 修复错误、纠正问题 |

**示例：**

色 深色主题朱砂色微调，印红更沉稳
暮色卷中关键字色值由 #D95B4A 调至 #C8524A，
更贴近古印泥之沉着质感。

笺 缘起重书，适配 v1.3 新增章节
版本纪事添 v1.3.1 条目，四象配色表更新语义高亮说明。

墨 重整 base.json，公共配色抽离复用
如立范本而后临帖，深浅两卷共用底色配置，
后续维护只需修一处而全局皆应。

校 归正 git.blame 批注前景色
溯源批注原与正文同色，难以辨识。
今以朱砂小印之色钤于行侧，一目了然。